driver:
  name: terraform
  root_module_directory: test/fixtures
  command_timeout: 1000

provisioner:
  name: terraform

verifier:
  name: terraform
  systems:
  - name: local
    backend: aws

platforms:
  - name: terraform

suites:
  - name: default